import React from 'react'; 
import {Text, View, StyleSheet} from 'react-native';

const GameStateScreen = ({navigation}) => { 
    return (
        <View>
            <Text>GameStateScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default GameStateScreen;